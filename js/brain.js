"use strict";
/* ----------------------------------------------------------------
* Tribe's Brain
*/
function Brain(tribe) {
    var self = {
        tribe: tribe,
        session: tribe.session,
    };
    self.update = function() {
        if (self.tribe.isSettled) {
        }
        else {
        }
    }
    return self;
}
